<div class="badge_info">
  <p>In order to user our Prototype badge, copy and paste the following code onto your site:</p>

  <hr />
  <p>In the &lt;head&gt; element <%= clippy render :partial => 'prototype_code' %></p>
  <code>
    <%= (render :partial => 'prototype_code').nl2br %>
  </code>

  <hr />
  <p>In the &lt;body&gt; element <%= clippy render :partial => 'quip_container_code' %></p>
  <code>
    <%= render :partial => 'quip_container_code' %>
  </code>
</div>